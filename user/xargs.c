#include "kernel/types.h"
#include "user/user.h"
#include "kernel/param.h"
#include "kernel/stat.h"
#include <stddef.h>

int main(int argc, char *argv[])
{
    // char *args[3];
    // args[0] = "";
 
    // char passedArgs[100];
    // int argCount = 0;
    // int counter = 0;
    
    // while(read(0,&passedArgs[counter++],1)){
    //     if(passedArgs[counter-1] == '\n'){
    //         args[argCount++] = &passedArgs[counter-1];
    //         break;
    //     }
    // }
    

    // fprintf(1,"first: %s  second: %s\n", args[0], args[1]);
    return 0;
}